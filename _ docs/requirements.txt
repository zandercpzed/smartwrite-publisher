# Requirements for smartwrite-publisher development

- Node.js (v16 or higher)
- npm or yarn
- TypeScript (standard for Obsidian plugins)
- Obsidian.md (for testing)

## Main Dependencies (to be added via npm):
- obsidian (API types)
- @types/node
- (potentially) axios or similar for HTTP requests to Substack
